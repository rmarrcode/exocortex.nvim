-- Template for lua/exocortex/config.lua (which is gitignored). Copy this file
-- to config.lua and fill in your own values.
return {
  -- Directory where finished chats are mirrored as Obsidian notes. Nodes that
  -- are connected in the graph view become [[wiki-links]] between the notes,
  -- so the Obsidian graph mirrors the exocortex DAG. Set to "" to disable.
  OBSIDIAN_DIR = "",
}

